const Joi = require('joi')

const schemas = {

    // decide: Joi.object().keys({
    //     address: Joi.string().required(),
    // }),

    // me: Joi.object().key({
    //     address: Joi.string().required(),
    // })
}

module.exports = schemas;
